import React, { useState } from 'react';
import ReactDOM from 'react-dom';

function Footer() {
    //Header
    return (
        <footer>
            <h6 className="fixed-bottom font-weight-light m-2 text-center border-top pt-2">&#169; Anthony Chen</h6>
        </footer>
    );
}

export default Footer